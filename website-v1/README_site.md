# GhostLink Site
Static landing page for GhostLink (single-file HTML).
- Build: none (static).
- Deploy: `netlify deploy` or any static host; publish directory `site/`.
- PDFs auto-linked from `dist/` if present.
