terraform {
  required_version = ">= 1.3.0"
}

# Placeholder: configure a provider for your preferred static host.
# Example: Cloudflare Pages (via cloudflare provider), Netlify (community), AWS S3+CloudFront, etc.

# variable "project_name" { default = "ghostlink-site" }
# variable "account_id"   { type = string }
# variable "zone_id"      { type = string }

# resource "cloudflare_pages_project" "ghostlink" { ... }
